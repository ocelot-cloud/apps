package main

import (
	"github.com/ocelot-cloud/shared/utils"
)

var (
	appsDir = "."
	logger  = utils.ProvideLogger("info")
)

func applyWhitelistToApps(apps []string, appWhitelist []string) ([]string, error) {
	return nil, nil
}
